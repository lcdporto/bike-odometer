services:
  backend:
    image: ${BACKEND_IMAGE:-ghcr.io/gpereira/bike-counter-backend:latest}
    ports:
      - "3000:3000"
    environment:
      NODE_ENV: production
      HOST: 0.0.0.0
      PORT: 3000
    volumes:
      - ./data:/app/data
    restart: unless-stopped